<Window x:Class="BPMNVisualizer.ChoiceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}" Height="300" Width="300" WindowStartupLocation="CenterOwner">
    <StackPanel Margin="10">
        <TextBlock Text="{Binding Message}" Margin="0,0,0,10" FontWeight="Bold"/>
        <ListBox x:Name="OptionsList" SelectionMode="{Binding SelectionMode}" SelectionChanged="OptionsList_SelectionChanged"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Select" Width="70" Margin="0,0,5,0" Click="Ok_Click"/>
        </StackPanel>
    </StackPanel>
</Window>